import { EndpointParameter } from './endpoint-parameter.model';

describe('EndpointParameter', () => {
  it('should create an instance', () => {
    expect(new EndpointParameter()).toBeTruthy();
  });
});
