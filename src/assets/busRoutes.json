{
  "endpoints": [
    {
      "method": "GET",
      "path": "/buses",
      "summary": "Search buses or get all buses",
      "tags": ["Buses"],
      "parameters": [
        {
          "in": "query",
          "name": "busNumber",
          "type": "string",
          "description": "The bus number to filter by"
        },
        {
          "in": "query",
          "name": "capacity",
          "type": "number",
          "description": "The capacity of the bus to filter by"
        }
      ],
      "responses": [
        { "status": 200, "description": "List of buses matching the search criteria" },
        { "status": 500, "description": "Internal server error" }
      ]
    }
  ]
}
